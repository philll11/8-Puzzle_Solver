#include <iostream>
#include "hash_function_test.h"

using namespace std;

int main() {

	// ##### Rolling hash function



	// string testState = "628035471";
	// // long long result = compute_hash(testState);
	// // cout << result << endl;
	// int VLength = 0;
	// vector<string> vl;
	// string val = bfs(testState, myGoalState, VLength, vl);

	// cout << "\nVisited list length: " <<  VLength << endl;
	// cout << "vl.size(): " << vl.size() << endl;
	/*
	for(vector<string>::iterator it = vl.begin(); it != vl.end(); ++it) {
		cout << *it << " ";
	}
	cout << endl;
*/
	
	return 0;
}